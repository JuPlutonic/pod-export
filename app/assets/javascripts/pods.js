// $(document).ready(function()) {
//     $('form.new_pod').bind('ajax:success',
//         function(e, data, status, xhr) {
//             response = $.parseJSON(xhr.responseText);

//             $('.pod-id') .html('<%#= response.pod_id %>')
//             $('.pod-organization') .html('<%#= response.pod_organization %>')
//     }
//    )
// }
